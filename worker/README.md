# שירות הביניים של דשבורד הכתבות

הקובץ `cms-relay.js` הוא שירות קטן שרץ ב-Cloudflare. הוא קיים מסיבה אחת:
האתר סטטי והתוכן שלו יושב במאגר בגיטהאב, והדפדפן לא יכול להחזיק מפתח
כתיבה למאגר, כי כל אחד יראה אותו בקוד הדף. השירות מחזיק את המפתח במקומו.

זו אותה תבנית בדיוק שכבר רצה באתר עבור כפתור הפוש (`push-relay`).

## הקמה חד פעמית

**1. אסימון גיטהאב**
GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens
- Repository access: **רק** `calendboard/tzadik-magor-site`
- Permissions → Repository permissions → **Contents: Read and write**
- בלי שום הרשאה נוספת. תוקף שנה.

**2. סיסמת הצוות**
לייצר במנהל הסיסמאות, לפחות 20 תווים אקראיים. זו הסיסמה שהצוות מקליד.

**3. מחרוזת חתימה**
עוד מחרוזת אקראית, נפרדת מהסיסמה. משמשת לחתימת כרטיסי הכניסה.

**4. Cloudflare**
Workers & Pages → Create → Worker → בשם **`cms-relay`** (השם חייב להיות זה,
הדשבורד פונה ל-`cms-relay.hatzadikmagor.workers.dev`).
להדביק את תוכן `cms-relay.js` ולפרוס.

Settings → Variables and Secrets:

| שם | סוג | ערך |
|---|---|---|
| `CMS_PASSWORD` | Secret | סיסמת הצוות משלב 2 |
| `CMS_SIGNING` | Secret | המחרוזת משלב 3 |
| `GITHUB_TOKEN` | Secret | האסימון משלב 1 |
| `GITHUB_REPO` | Variable | `calendboard/tzadik-magor-site` |

Settings → Bindings → KV namespace:
ליצור namespace בשם `cms-rate-limit` ולקשר אותו בשם המשתנה **`RATE`**.
בלעדיו הכניסה תיכשל, וזה מכוון: בלי KV אין הגבלת ניסיונות סיסמה.

## מה קורה אחרי שמירה

הדשבורד → השירות כותב ל-`data/news.json` → GitHub Action `build-site.yml`
בונה מחדש את `a/<id>.html` ואת `sitemap.xml` → GitHub Pages מפרסם.
בסך הכל 2 עד 5 דקות מרגע השמירה ועד שזה באתר.

## מה מוגן ואיך

- **הסיסמה נבדקת רק בשרת**, בהשוואה בזמן קבוע. היא לא נמצאת בקוד האתר.
- **הגבלת ניסיונות**: 8 כשלונות מאותה כתובת IP חוסמים אותה לרבע שעה.
- **כרטיס כניסה זמני**: אחרי כניסה מוצלחת נשלח כרטיס חתום שתקף 8 שעות,
  כך שהסיסמה עצמה לא נשלחת בכל פעולה.
- **סוגי קבצים**: רק תמונות, סרטונים והקלטות. קובץ HTML או קוד נדחה,
  אחרת אפשר היה להעלות דף זדוני לתוך הדומיין שלנו.
- **שמות קבצים**: נתיבים מנוטרלים, הקובץ תמיד נוחת ב-`assets/news/`.
- **גודל**: עד 25 מגה לקובץ. סרטונים כבדים עולים ליוטיוב.
- **הרשאת האסימון**: כתיבה למאגר הזה בלבד, בלי גישה לשום מאגר אחר.

## קיר הנרות (נרות, תפילות, ישועות, פניות ופדיון)

אותו שירות מטפל גם בקיר הנרות. עד עכשיו הקיר ישב בקופסת JSONBin שהייתה
**פתוחה לכתיבה לכל העולם בלי מפתח**: כל מי שהכיר את כתובת הקופסה יכול היה
למחוק את כל הרשומות, להשחית את הקיר, או לפרסם "ישועה" מזויפת. עכשיו הקופסה
נעולה לפרטית, והשירות הוא היחיד שמחזיק את המפתח.

### נקודות הקצה

| נתיב | מי | מה |
|---|---|---|
| `POST /wall/list` | ציבורי | מחזיר את כל רשומות הקיר (הקוד באתר קורא מכאן) |
| `POST /wall/append` | ציבורי, מוגבל בקצב | הוספת נר/תפילה/ישועה/פנייה מהקהל |
| `POST /wall/admin` | דורש כרטיס כניסה | עריכה, מחיקה, הוספה ידנית ואישור ישועה |

### מה מוגן ואיך

- **הקופסה פרטית**: אי אפשר לקרוא או לכתוב אליה בלי מפתח, והמפתח רק בשרת.
- **הגבלת קצב**: עד 12 הוספות מאותה כתובת IP בתוך 10 דקות (אותו KV של הכניסה).
- **ישועה נכפית ל-pending**: רשומה מהקהל תמיד ממתינה לאישור, גם אם נשלח
  `status: "approved"`. פרסום באתר אפשרי רק דרך `/wall/admin` המאומת.
- **רק שדות מותרים**: הוספה מהקהל מנקה את הרשומה - רק השדות הגלויים שכל סוג
  מכיר, ומחרוזות מוצפנות (`encb`/`enc`). המזהה והתאריך נקבעים בשרת.
- **פדיון לא נפתח להוספה ציבורית**: נוסף רק מהדשבורד המאומת.
- **עריכה ומחיקה דורשות סיסמת צוות** - אותה סיסמה של דשבורד הכתבות
  (`CMS_PASSWORD`), אותו כרטיס כניסה חתום.

### הקמה חד פעמית (מה שצריך לעשות ב-Cloudflare)

1. **לנעול את הקופסה לפרטית** ב-JSONBin (Bin → Settings → Private). מרגע זה
   כל קריאה וכתיבה דורשות את ה-Master Key.
2. להוסיף לשירות `cms-relay` הקיים שני משתנים (Settings → Variables and Secrets):

   | שם | סוג | ערך |
   |---|---|---|
   | `JSONBIN_KEY` | Secret | ה-X-Master-Key של חשבון ה-JSONBin |
   | `JSONBIN_BIN` | Variable | `6a40f90fda38895dfe0b10e7` |

3. לפרוס מחדש את `cms-relay.js`. הכתובת נשארת אותה כתובת
   (`cms-relay.hatzadikmagor.workers.dev`), והאתר כבר מכוון אליה.

לפני שנועלים את הקופסה כדאי לגבות אותה: יש גיבוי טרי בתיקייה
`Desktop/הצדיק מעג׳ור /גיבוי-קיר-הנרות/`.

## בדיקות

```
node worker/test_worker.mjs   # לוגיקת דשבורד הכתבות מול מאגר מדומה
node worker/test_wall.mjs     # לוגיקת קיר הנרות מול קופסה מדומה
node worker/test_client.mjs   # השרשרת המלאה: דשבורד -> שירות -> מאגר
```
